# Requirements that don't require compilation - Windows compatible
# Uses only packages with pre-compiled wheels available

# Core GUI framework
PyQt6==6.6.1

# Schema validation (has pre-compiled wheels for Windows)
xmlschema==3.3.0

# Type hints (lightweight, no compilation needed)
typing-extensions==4.8.0

# Note: Using built-in xml.etree.ElementTree instead of lxml
# This avoids compilation issues on Windows