# Minimal requirements for ARXML Editor - Windows compatible
# Only includes packages that are actually used and have pre-compiled wheels

# Core GUI framework
PyQt6==6.6.1

# XML processing (has pre-compiled wheels for Windows)
lxml==4.9.3

# Schema validation (has pre-compiled wheels for Windows)
xmlschema==3.3.0

# Type hints (lightweight, no compilation needed)
typing-extensions==4.8.0